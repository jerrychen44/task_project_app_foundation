<!-- add for nested routes, chagne  form_for(@task) to  form_for[@project , @task] -->
<!-- change to an array -->
<%= form_for [@project, @task] do |f| %>
  <!--error handle first-->
  <% if @task.errors.any? %>
    <%= pluralize(@task.errors.count,"error") %> prohibited this project from being saved:
    <%= @task.errors.full_messages.each do |message| %>
      <%= message %>
    <% end %>
  <% end %>



    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :description %>
    <%= f.text_area :description %>

    <%= f.label :completed %>
    <%= f.check_box :completed %>

    <%= f.label "Attachement" %>
    <%= f.file_field :task_file %>

    <%= f.submit %>


  <% end %>
